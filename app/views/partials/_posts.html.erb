<% @posts.each do |post| %>
  <div class="w-1/2">
    <div class="my-5 mx-4 py-5 px-5 bg-gray-300">
      <div><%= post.id %></div>
      <div><%= post.title %></div>
      <div><%= post.content %></div>
    </div>
  </div>
<% end %>
